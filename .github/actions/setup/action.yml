name: Setup
description: Checkout-independent setup steps shared across CI jobs

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version-file: package.json
        cache: npm

    - name: Install dependencies
      shell: bash
      run: npm install